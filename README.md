# RL Tools

This repo consist of tools for connection with game engine via currently implemented methods:

- sockets.

In addition class for running headless instances of game engines for faster training is here - WIP. Modules are created in OOP model to ensure that each interface can be swapped with other method for increased support of other engines and big changes allowance.